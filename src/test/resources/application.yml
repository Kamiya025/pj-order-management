server:
  port: 8025
spring:
  application:
    name: Project Order Management Application
  datasource:
    url: jdbc:mysql://localhost:3306/test-fis-training?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password:
  jpa:
    show-sql: true
    generate-ddl: true
